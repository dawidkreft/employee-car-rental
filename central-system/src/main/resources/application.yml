server:
  port: 1020

spring:
  profiles.active: prod
  datasource:
    name: employee_car_rental_db
    url: jdbc:postgresql://localhost:5432/employee_car_rental_db
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
